# Xenova/paraphrase-multilingual-MiniLM-L12-v2

Put the downloaded model files for `Xenova/paraphrase-multilingual-MiniLM-L12-v2` in this directory.
The app runs in offline mode with `env.allowRemoteModels = false`, so these files must exist before packaging.
