# ONNX Runtime Web WASM files

Run `npm run prepare:transformer-resources` after `npm install` to copy ONNX Runtime Web `.wasm` files here from `node_modules`.
These files are included in the packaged app via `build.extraResources`.
