# v457 Editor AI placement

- The Workspace AI orb remains the fixed bottom-right control (`right/bottom: 22px`).
- BlockNote editor AI uses an independent slot above it (`bottom: 88px`).
- The editor panel shares the upper slot and does not occupy the Workspace AI orb position.
- Mobile uses the same stacked layout with a 78px bottom offset.
