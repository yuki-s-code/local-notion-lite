# v410 タグ別名・表記ゆれ

タグ候補は、現在の端末に保存した別名・表記ゆれも参照する。

- 例: `放課後児童クラブ` に `学童, 学童保育` を登録
- 本文・タイトルの別名に一致すると、既存タグ `#放課後児童クラブ` を候補表示
- 直接のタグ一致より別名一致は弱い
- 設定は Renderer の localStorage にのみ保存され、共有フォルダ・他端末へは同期しない
- 自動付与はしない
