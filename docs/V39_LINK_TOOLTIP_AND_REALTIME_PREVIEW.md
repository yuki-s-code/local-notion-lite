# v39 Link tooltip and realtime preview

- Removed the page-level "リンク表示" toolbar.
- Existing links can be converted between inline/card from a small tooltip opened by clicking the link.
- Clicking a local page link now opens the right preview drawer immediately.
- The preview drawer is now a fixed right-side drawer instead of an implicit grid column, so it does not wait for window resize/reflow.
