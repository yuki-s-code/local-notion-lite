# v72 menu delay and local file attachment

- Page tree option menus no longer close immediately when hovering another row.
- A small delay is added, and menus stay open while the pointer is on the menu.
- BlockNote slash menu includes local file attachment. Selected local files are copied to the shared vault attachments folder and inserted into the page body as file links.
