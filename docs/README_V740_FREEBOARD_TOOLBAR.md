# v740 — Freeform-style compact whiteboard toolbar

- Replaced the wide text toolbar with an icon-only floating palette.
- Moved zoom controls into a separate bottom-right glass dock.
- Added hover/focus tooltips and accessible labels.
- Added a compact shape picker that appears only for the shape tool.
- Added narrow-screen sizing so labels never truncate to one or two characters.
