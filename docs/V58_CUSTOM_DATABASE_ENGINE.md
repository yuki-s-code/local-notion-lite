# V58 Custom Database Engine

TanStack Table / Recharts を外し、データベース画面を自作の軽量仮想スクロールテーブルへ置き換えました。

## 方針

- 表示中の行だけ描画する仮想スクロール
- セル入力中はセル内部 state のみ更新
- 親DBへの反映は遅延保存
- 分析はパネルを開いた時だけ計算
- 列幅・列表示は localStorage に保存
- Recharts の代わりに軽量CSSバーで分析を表示

## 目的

文字入力ごとの全再レンダリングを避け、複雑なデータベースでも使える土台にするためです。
