# v63 Sidebar overflow and embedded database editing

- Fixed sidebar overflow where Template / Filter controls were partially clipped.
- Adjusted trash quick action so the count no longer pushes outside the button.
- Kept the compact database list balance from v62.
- Embedded databases inside a page are now editable from the BlockNote page context.
- Embedded database edits are saved through the existing database save API and synced back to the workspace database list.
