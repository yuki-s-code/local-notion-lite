# V647 Global Map Isolated Nodes

- Keep page and database-row nodes without visible edges in the global knowledge map.
- Preserve tags only when they have a visible connection to avoid visual noise.
- Show the count of isolated items in the map status bar.
- Focus mode remains intentionally edge-driven, so isolated items disappear only while focus is enabled.
