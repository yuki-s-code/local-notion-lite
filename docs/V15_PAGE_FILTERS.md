# v15 Page Filters

ページプロパティを一覧・検索結果で使えるようにしました。

## 追加内容

- サイドバーにページフィルターを追加
- ステータス / 優先度 / 担当者 / タグ / 期限範囲 / ロック状態 / 期限切れで絞り込み
- ページツリーと検索結果の両方にフィルターを適用
- ページ一覧にプロパティバッジを表示

## 方針

共有フォルダの正本データは従来通り `pages/{pageId}/meta.json` です。
SQLiteは検索・表示用キャッシュとして使います。
