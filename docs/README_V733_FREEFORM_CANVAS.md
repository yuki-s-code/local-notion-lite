# v733 Freeform Canvas

追加内容:

- ホワイトボード / Freeform風キャンバスを追加
- ページカード、DBカード、付箋、グループ枠をキャンバスに配置可能
- カードのドラッグ移動、色変更、本文編集、削除に対応
- 2つのカードを選択して線で接続可能
- Cmd+K / サイドバー / ホームから開ける導線を追加

効率化方針:

- サーバーAPIや共有フォルダI/Oは追加せず、localStorageに軽量保存
- 保存は220msのdebounceで自動保存
- ページ/DB候補は既に読み込まれている `allVisiblePages` / `databases` を再利用
- 既存のナレッジマップとは重複させず、AI自動生成グラフではなく「手動で考えを配置するキャンバス」として分離
- 新規ライブラリ追加なし

確認:

- CSSチェック OK
- src配下の生成済み .js なし
