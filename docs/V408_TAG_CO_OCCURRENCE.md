# v408 タグ候補：併用傾向

v408では、既存タグの文字一致に加えて、現在付いているタグと過去ページで一緒に使われたタグを補助候補として表示します。

- 本文・タイトル一致が最優先
- 併用候補は、同じ組み合わせが2ページ以上ある場合だけ表示対象
- 候補の表示だけで、自動追加はしない
- すでにページに付いているタグは常に除外
- 候補には `#既存タグ と関連・併用N件` と根拠を表示

例：`#放課後児童クラブ` が既に付いたページで、過去の複数ページが `#夏休み` と併用していれば、本文に「夏休み」と書かれていなくても `#夏休み` が補助候補になります。
