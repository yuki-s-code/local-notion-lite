# V649 Knowledge Map Structuring

- Per-cluster local names and collapse/expand controls; preferences remain in localStorage.
- Health panel lists isolated documents and old high-reference documents without extra server requests.
- Existing shortest-path mode now shows shared tags between the two selected documents.
- Export uses the already-rendered SVG only; no graph rebuild or full-document scan.
- Cluster membership remains memoized from the visible bounded graph; node movement only changes geometry.
