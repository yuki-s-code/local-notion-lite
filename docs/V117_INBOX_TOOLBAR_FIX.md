# v117 Inbox and BlockNote toolbar fix

- Improves the sidebar Inbox card width so titles/text remain readable while staying inside the sidebar.
- Removes the custom `FormattingToolbarController` that interfered with BlockNote/Tiptap toolbar click handling.
- Keeps the Alert block available from the slash menu and preserves the standard BlockNote formatting toolbar behavior.
