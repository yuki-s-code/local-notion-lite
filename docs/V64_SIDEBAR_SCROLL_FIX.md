# v64 Sidebar Scroll Fix

テンプレートやフィルターを展開した時に、サイドバー内で下部が見切れてスクロールできない問題を修正しました。

## 変更点

- サイドバーを `brand / scroll body / local info` の3領域に分割
- 中央の `sidebar-scroll-body` だけを縦スクロール可能化
- テンプレート・フィルター展開時も内容が切れないように調整
- ゴミ箱ボタンの数値表示が枠からはみ出しにくいように再調整
- 本文・DB・右サイドバーの既存挙動は維持
