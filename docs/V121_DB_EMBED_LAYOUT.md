# v121 DB embed candidate and layout polish

- Slash menu now shows a single database launcher by default instead of dumping all databases into the first slash list.
- Typing `/database name` or `/db name` still filters database candidates directly in the slash menu.
- Clicking "データベースを埋め込み" opens the larger database picker near the lower part of the editor so many candidates do not cover the writing area.
- Embedded databases are rendered below the BlockNote editor in the main writing column, not inside the narrow outline utility rail.
- The right utility rail is reserved for the outline.
