# v534 Compact Sticky Workspace Rail

- The page/database tab rail is now a compact 34px strip attached to the top of the editor area.
- The active item's command bar is directly below it and uses the same sticky behavior for page and database tabs.
- The page body and database content now scroll beneath the shared workspace header.
- The old rounded card gap above the tab rail was removed; the rail spans the editor area edge-to-edge.
- Database headers remain content headers and do not create another global sticky action row.
