# v424 通常検索・サイドバーのタグ絞り込み

- サイドバー検索欄の下にタグチップを表示します。
- タグを選ぶと、ページ一覧を即時にタグで絞り込みます。
- 複数タグを選択した場合は AND 条件です。
- テキスト検索と組み合わせる場合は、先に既存の全文検索を実行し、その結果をタグでさらに絞り込みます。
- タグだけで検索する場合は、すでに読み込まれているページメタデータだけを使うため、サーバー検索を追加で実行しません。
- これは表示・検索状態のみの変更で、ページ、タグ、履歴は書き換えません。
