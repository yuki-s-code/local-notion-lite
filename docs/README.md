# Documentation

Project documentation belongs under `docs/`.

- `release-notes/`: implementation notes, audit records, and version-specific change documentation.
- Existing topical documents remain at the top level of `docs/` where they are already grouped by feature.

Keep the repository-root `README.md` as the project entry point. Do not add new implementation, audit, or release-note Markdown files at repository root.
