# v33 Inline Link Fix

- `@ @[[title|page_id]]` のように表示される問題を修正しました。
- `@タイトル` 入力中に候補を選ぶと、入力中の `@タイトル` を直接ページリンクへ置換します。
- 旧形式 `@[[title|page_id]]` と誤挿入された `@ @[[title|page_id]]` は読み込み時にBlockNoteのリンク表示へ正規化します。
- 本文では `📄 タイトル` のように見え、クリックすると対象ページを開きます。
