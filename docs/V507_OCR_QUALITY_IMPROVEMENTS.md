# v507 OCR 精度改善

- OCR実行時に「標準」「高精度」を選択可能。
- 高精度はImageMagickが利用できるPCで、自動傾き補正・グレースケール化・コントラスト補正・二値化を行う。
- ImageMagickが未設定の場合は元画像で安全にOCRを継続し、結果に理由を表示。
- OCR文字列は段落・改行を簡易整形。
- 手書き・写真メモは確認が必要な注意を表示。
- AIで整理は既存AIチャットを開き、要約・タグ候補・FAQ候補・誤認識候補を提案する。結果は自動適用しない。
