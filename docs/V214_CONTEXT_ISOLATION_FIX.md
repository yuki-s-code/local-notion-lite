# v214 Context Isolation Fix

- チャット履歴を常に検索クエリへ混ぜる挙動を停止。
- UIの「履歴を元に続ける」がONのときだけ context を送信。
- サーバー側も useContext=true のときだけ文脈補完を許可。
- 「学童クラブの費用」「減免について」など主題語が明示された質問では文脈を混ぜない。
- 文脈補完は「それ」「その」「さっきの続き」など単独では対象が不明な短い質問だけ。
