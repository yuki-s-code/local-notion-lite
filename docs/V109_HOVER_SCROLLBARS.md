# v109 Hover Scrollbars

- Scrollbar thumbs are visually hidden by default.
- Scrollbar thumbs fade in on hover over scrollable areas.
- Works with Electron/Chromium via `::-webkit-scrollbar` and Firefox via `scrollbar-color`.
- Keeps `scrollbar-gutter: stable` on key scroll containers to reduce layout shift.
