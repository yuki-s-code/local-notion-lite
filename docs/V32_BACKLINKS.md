# v32 Backlinks and clickable page links

- `@` で挿入したページリンクはBlockNote内でクリック可能なリンクとして保存します。
- Markdownミラーには `@[[タイトル|page_id]]` を保持し、共有フォルダ検索・履歴・バックリンク抽出に使います。
- プロパティ > リンク タブで、このページへのバックリンクと、このページからのリンクを確認できます。
- 旧形式 `@[[タイトル|page_id]]` と、過去の `📄 タイトル` 形式も読み取り対象です。
