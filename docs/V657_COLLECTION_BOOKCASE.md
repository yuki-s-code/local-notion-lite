# V657 — 資料コレクション棚の本棚化

- 棚そのものを選択可能にし、選んだ棚を下部の「開いた本棚」として展開する。
- 本の背表紙をクリックすると既存の `onOpenPage` / `onOpenDatabase` / `onOpenJournal` を通じて対象を開く。
- 棚をクリックしても何も起きない曖昧なUIをなくし、棚の選択状態・開く導線・資料削除操作を明示した。
- 新しい索引、全件検索、共有データ更新は追加しない。棚は端末内localStorageのみで保持する。
