# v55 データベース表示フリーズ対策

## 修正内容

- TanStack Table の表示行を一度に最大500行へ制限
- 表示行・検索結果・ビュー適用結果を `useMemo` でメモ化
- 分析処理をパネルを開いた時だけ実行
- 列リサイズを `onChange` から `onEnd` に変更し、ドラッグ中の大量再描画を抑制
- テーブル列定義の再生成を抑制

## 方針

データベース全体のJSON構造は変更していません。共有フォルダ内の既存データはそのまま利用できます。
