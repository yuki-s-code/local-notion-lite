# v477 Workspace Layout Hotfix

- Split reference pane now reserves editor space on wide screens, preventing overlap.
- At medium widths, the reference pane docks to the bottom and reserves vertical space.
- Tab close control is always visible (subtle), including pinned and active tabs.
- The final active tab remains visible but disabled to prevent an empty workspace.
