# v628 クロスタブPivotのDuckDB移管

- クロスタブPivotは、上流結果をRendererへ全件取得せずDuckDB上で実行する。
- 列候補の取得と集計は同一の一時テーブル・同一接続で行い、上流結果を二重に転送しない。
- 列項目は64種類までに制限し、過大な横持ち表によるメモリ・描画負荷を防ぐ。
- 通常の単純Pivotは既存のSQL経路を継続する。
- 上流結果キャッシュの期限切れ時は既存と同じ再実行要求を返す。
