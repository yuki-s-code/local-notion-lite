# V545 — DB子ページタイトル同期

DB行の子ページタイトルを変更した際、サイドバーの子ページ一覧だけでなく、DB行本文に保存される `@[[タイトル|ページID]]` とBlockNoteの `local-page://` リンク表示名もID基準で更新します。

対象は親子関係（`database-row:<DB ID>:<行 ID>`）が確認できる子ページだけです。任意に付けた通常リンクの表示名は変更しません。DB行本文を開いている場合も、未保存編集がないときだけ自動再読込して新しいタイトルを反映します。
