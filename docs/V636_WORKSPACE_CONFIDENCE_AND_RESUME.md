# V636 保存・同期の見える化と作業再開

## 追加内容
- サイドバーに常時表示する保存・共有同期ステータスを追加しました。
- ページ、データベース、Journalの保存中状態と、共有フォルダ同期中・同期エラーを明示します。
- 保存失敗で再試行対象がある場合は、その場で再試行できます。
- 最近開いたページ・データベース・Journalをローカルに最大18件保持し、ホームから閉じたタブも含めて再開できます。

## 保存範囲
作業再開履歴は端末ごとのUI補助情報として localStorage にのみ保存します。共有フォルダや他利用者には共有されません。
