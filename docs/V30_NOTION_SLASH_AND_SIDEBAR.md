# v30 Notion Slash and Sidebar

- Removed existing page-link candidates from the BlockNote slash menu.
- `/` now exposes a Local Notion item for creating a child page under the current page.
- `@` remains the intended way to link existing pages.
- Added a Notion-like sidebar show/hide toggle. The state is saved in localStorage.
