# V102 BlockNote paged link suggestions

- BlockNote editor `@` page suggestions no longer render all page candidates at once.
- The menu initially shows up to 24 pages.
- A `さらに表示` item loads 24 more candidates at a time.
- Inline link and card link insertion remain available.
- This prevents large workspaces from making the editor sluggish when typing `@`.
