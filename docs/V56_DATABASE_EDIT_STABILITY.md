# v56 Database edit stability

- TanStack Table のセル編集で古い database クロージャを参照し、追加直後の行が消えたように見える問題を修正。
- 追加・複製・編集中の行を pinnedRowIds として一時的に表示対象へ固定。
- 検索/フィルター条件に合わない空行でも、編集中はテーブル上部へ残す。
- セル編集時に対象行を pin し、再描画後も選択行・編集中行が見失われないようにした。
